{
  "name": "evenly-root",
  "private": true,
  "scripts": {
    "build": "pip install -r requirements.txt --break-system-packages && alembic upgrade head && cd frontend && npm install && npm run build"
  }
}
